ktor:
  application:
    modules:
      - com.suprbeta.ApplicationKt.module
  deployment:
    port: 8080
  development: false

# VPS/Production deployment configuration
deployment:
  environment: vps
  ssl:
    enabled: true
