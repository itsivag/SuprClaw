ktor:
  application:
    modules:
      - com.suprbeta.ApplicationKt.module
  deployment:
    port: 8080
    watch:
      - com.suprbeta
  development: true

# Deployment environment configuration
deployment:
  environment: local  # Options: local, vps
  ssl:
    enabled: false    # Set to true for production/VPS with SSL certificates
